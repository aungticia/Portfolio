extends layout.pug

block content

    div.error-message 
        h1 #{err.status}
        h2= `${err.message}`
        pre= `${err.stack}`

    div 
        nav.nav
            a(href='/')
              span &larr;
              |  Back